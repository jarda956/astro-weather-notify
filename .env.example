# Copy this file to .env and adjust values before starting the app.

# Port the web server listens on
PORT=3000

# Secret used to sign login sessions (JWT). Generate with: openssl rand -hex 32
JWT_SECRET=change-me-to-a-long-random-string

# Where the SQLite database file is stored
DATABASE_PATH=./data/astro-weather.sqlite

# Telegram bot token from @BotFather. Leave empty to disable Telegram notifications.
TELEGRAM_BOT_TOKEN=

# Cron expression for how often to check forecasts and send notifications (default: every hour)
NOTIFY_CRON=0 * * * *

# How many hours before sunset the app is allowed to send a "clear sky tonight" notification
NOTIFY_LOOKAHEAD_HOURS=6

# Testing only: set to "true" to make the scheduler evaluate every location on every cron
# tick regardless of time of day (normally it only checks in the NOTIFY_LOOKAHEAD_HOURS
# window before sunset, plus 1 hour after). Leave false/unset in normal operation.
NOTIFY_IGNORE_WINDOW=false
